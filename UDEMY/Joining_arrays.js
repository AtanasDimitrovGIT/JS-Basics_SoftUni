const numbers = [1, 2, 3];
const joined = numbers.join(",");
console.log(joined);

// use .join to add an element in between every element in the array
// use .join("-") to merge splitted strigs into an array