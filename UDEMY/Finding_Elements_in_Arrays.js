const numbers = [1, 2, 3, 4];

// use indexOf() to find the index of the element 

console.log(numbers.indexOf(2));

// use .includes() to check if an element exist in the array

console.log(numbers.includes(3));