const numbers = [1, 2, 3];
numbers.sort();
console.log(numbers);

// use .sort() to sort numbers from lowest to highest


const numbers1 = [1, 2, 3];
numbers1.reverse();
console.log(numbers1);

// use .reverse() to reverse the numbers from highest to lowest 